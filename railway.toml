[build]
builder = "nixpacks"
buildCommand = "npm install && npx turbo run build --filter=@artist/web"

[deploy]
startCommand = "cd apps/web && npm start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[service]
internalPort = 3000
